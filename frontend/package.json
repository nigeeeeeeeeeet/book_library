{
  "name": "book-library-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild liquid-buttons.src.js --bundle --minify --format=iife --target=es2020 --outfile=../app/static/liquid-buttons.js"
  },
  "dependencies": {
    "@paper-design/shaders": "0.0.81"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
